import { BoardgameNoteDirective } from './boardgame-note.directive';

describe('BoardgameNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardgameNoteDirective();
    expect(directive).toBeTruthy();
  });
});
